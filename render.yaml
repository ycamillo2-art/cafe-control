services:
  - type: web
    name: cafe-control
    env: node
    buildCommand: npm run install-all && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cafe-db
          property: connectionString

databases:
  - name: cafe-db
    plan: free
